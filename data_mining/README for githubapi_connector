README for githubapi_connector
Author: Ivaylo Petrov
Login: ipetrov

Put your Github API token on line 6 to make the code work!

default_fields global variable is not yet working, which means that when we connect to the API at a request, we access data for all properties rather than specific ones (such as login and id). Thus, we filter for those properties that we want when we write to the .csv file.